import React from 'react';
import Transaction from '../../../assets/img/icons/transaction-icon.png';

const TransactionIcon = () => {
	return <img src={Transaction} alt={''} width={160} height={110} />;
};

export default TransactionIcon;
