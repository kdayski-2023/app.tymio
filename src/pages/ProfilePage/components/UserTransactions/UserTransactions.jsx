import React from 'react';
import UserOrder from './UserOrder';
import * as Styled from './styled';

const UserTransactions = () => {
	return (
		<Styled.UserTransactions>
			<UserOrder />
		</Styled.UserTransactions>
	);
};

export default UserTransactions;
